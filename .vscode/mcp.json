{
  "servers": {
    "axon": {
      "type": "stdio",
      "command": "${workspaceFolder}/target/debug/axon",
      "args": [
        "serve",
        "--workspace",
        "${workspaceFolder}",
        "--web-port",
        "3769"
      ]
    }
  }
}
